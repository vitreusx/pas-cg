# On native and non-native contacts

We distinguish between the native contacts, whose interactions are **solely**
handled by the native contacts force field, and all other pairs of non-local
residues. By _non-local_, we mean such that are separated by at least 2 other
residues.